-- Keybindings defaults

local keys = {}

-- movement
keys.walk_up                    = "w"
keys.walk_down                  = "s"
keys.walk_left                  = "a"
keys.walk_right                 = "d"

-- interaction
keys.interact                   = "e"

-- ui
keys.menu                       = "escape"

return keys